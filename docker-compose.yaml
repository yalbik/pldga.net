version: '3.8'
services:
  pldga:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - pldga-data:/home/App_Data
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - APP_DATA_PATH=/home/App_Data
    restart: unless-stopped

volumes:
  pldga-data: